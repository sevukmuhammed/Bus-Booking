<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bkturizm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".DisplayTicketsActivity"></activity>
        <activity android:name=".Buy2Activity" />
        <activity android:name=".ReturnPrize2Activity" />
        <activity android:name=".Going2Activity" />
        <activity android:name=".ReturnPriceActivity" />
        <activity android:name=".BuyActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Gecici" />
        <activity android:name=".PriceActivity" />
        <activity android:name=".GoingActivity" />
        <activity android:name=".OneWayActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>